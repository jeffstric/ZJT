# 媒体文件缓存管理方案

实现生成完成的图片/视频自动缓存到本地，按日期目录组织，并支持超时/容量限制自动清理。

---

## 需求分析

**问题**：第三方平台返回的视频/图片URL有有效期，超期后无法访问。

**目标**：
1. 任务完成后自动下载媒体文件到本地 `upload/cache/` 目录
2. 按日期目录组织（如 `upload/cache/2026-02-27/`）
3. 支持配置：超时天数 或 总容量限制
4. 超限后自动删除最早的文件

---

## 方案设计

### 1. 目录结构

```
upload/
└── cache/
    └── 2026-02-27/
        ├── video_abc123.mp4
        └── image_def456.png
```

### 2. 配置项 (`config.example.yml`)

```yaml
media_cache:
  enabled: true                    # 是否启用缓存
  cache_dir: "upload/cache"        # 缓存目录（相对于项目根目录）
  max_days: 30                     # 文件保留天数（0=不限制）
  max_size_gb: 10                  # 最大缓存容量GB（0=不限制）
  cleanup_on_startup: true         # 启动时执行清理
  cleanup_interval_hours: 24       # 定时清理间隔（小时）
```

### 3. 核心模块设计

#### 3.1 新增文件：`utils/media_cache.py`

| 函数 | 功能 |
|------|------|
| `download_and_cache(url, task_id, media_type)` | 下载媒体文件并缓存到日期目录，返回本地URL |
| `get_cache_path(task_id, media_type, date)` | 获取缓存文件路径 |
| `cleanup_expired_files()` | 清理过期文件（按天数） |
| `cleanup_by_size()` | 按容量限制清理（删除最旧的） |
| `get_cache_stats()` | 获取缓存统计信息 |

#### 3.2 修改文件：`task/visual_task.py`

在 `_handle_task_success()` 中增加缓存逻辑：

```python
def _handle_task_success(project_id, task_id, media_url):
    # 1. 下载并缓存媒体文件
    from utils.media_cache import download_and_cache
    local_url = await download_and_cache(media_url, task_id, "video")
    
    # 2. 更新数据库（可选：同时保存原URL和本地URL）
    AIToolsModel.update_by_project_id(
        project_id=project_id,
        result_url=media_url,        # 原始URL
        cached_url=local_url,        # 本地缓存URL（需新增字段）
        status=AI_TOOL_STATUS_COMPLETED
    )
```

#### 3.3 修改文件：`config/constant.py`

新增缓存相关常量：

```python
class MediaCacheConfig:
    """媒体缓存配置常量"""
    DEFAULT_MAX_DAYS = 30
    DEFAULT_MAX_SIZE_GB = 10
    CACHE_SUBDIR = "upload/cache"
```

#### 3.4 定时清理任务：`task/scheduler.py`

新增定时清理任务，使用现有的调度器框架。

---

## 实现步骤

### 阶段1：基础缓存功能
1. 新增 `utils/media_cache.py` - 核心缓存模块
2. 更新 `config.example.yml` - 添加配置项
3. 更新 `config/constant.py` - 添加常量
4. 修改 `task/visual_task.py` - 集成缓存逻辑

### 阶段2：自动清理功能
5. 实现过期清理逻辑（按天数）
6. 实现容量清理逻辑（按大小）
7. 集成到调度器 `task/scheduler.py`

### 阶段3：数据库支持（可选）
8. 新增 `cached_url` 字段到 `ai_tools` 表（通过 Alembic 迁移）
9. 更新前端展示逻辑，优先使用本地缓存URL

---

## 技术要点

### 异步下载
使用 `aiohttp` 进行非阻塞下载，符合项目规范（禁止在异步函数中调用同步库）。

### 跨平台兼容
使用 `os.path.join()` 和 `pathlib` 处理路径，兼容 Windows/Linux/macOS。

### 文件命名
格式：`{task_id}_{timestamp}_{hash8}.{ext}`  
示例：`12345_20260227_abc12def.mp4`

### 清理策略
- **按天数**：删除 `mtime` 超过 N 天的文件
- **按容量**：当总大小超限时，按 `mtime` 从旧到新删除，直到低于阈值

---

## 确认的设计决策

1. **替换 result_url**：直接将 result_url 替换为本地缓存URL，无需新增数据库字段
2. **清理策略：并集**：超过天数 或 超过容量 任一满足即删除

---

## 管理后台入口

管理后台「媒体缓存」页面可视化配置以下项：`enabled` / `max_days`（保持时长） / `max_size_gb`（最大磁盘大小） / `cleanup_on_startup` / `cleanup_interval_hours`，写入 `system_config` 表，运行时通过 `get_dynamic_config_value` 优先读取。**`cache_dir`（缓存目录）不开放给用户配置**，固定使用 YAML/元数据默认值。详见 [admin_panels.md · 5. 媒体缓存管理](admin_panels.md)。