# 视频节点使用指南

## 概述

视频节点用于存储和管理视频文件，可以接收图生视频节点的输出，也可以直接上传视频。

## 功能

| 功能 | 说明 |
|------|------|
| 上传视频 | 支持本地视频文件上传 |
| 视频预览 | 点击预览按钮查看视频 |
| 添加到时间轴 | 将视频添加到时间轴进行编排 |
| 接收输入 | 通过连接线接收图生视频节点的输出 |

## 连接端口

| 端口 | 方向 | 说明 |
|------|------|------|
| 输入端口 | 左侧 | 接收图生视频节点的输出 |

## 使用方式

### 方式一：直接上传

1. 添加视频节点
2. 点击上传区域选择本地视频文件
3. 等待上传完成

### 方式二：连接图生视频

1. 先创建图生视频节点并生成视频
2. 从图生视频节点的输出端口拖拽连接到视频节点
3. 视频自动填充到节点中

## 注意事项

- 上传的视频会存储到服务器
- 视频文件大小受配置限制
- 添加到时间轴后可在时间轴面板中编排顺序
