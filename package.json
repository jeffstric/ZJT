{
  "name": "comfyui-server-frontend",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run --reporter=default --reporter=junit --outputFile=frontend-test-results.xml --passWithNoTests"
  },
  "devDependencies": {
    "vitest": "^3.2.0",
    "@vitest/coverage-v8": "^3.2.0",
    "jsdom": "^26.1.0"
  }
}
