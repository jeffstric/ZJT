done:

1. 【feature】线条从起点连接时，其他线条需要继续显示，不能隐藏。
1. 【bug】场景节点支持删除
1. 【feature】图生视频需要支持 运镜选项，分别为 向左横切、向右横切，镜头拉、镜头推。最好有图片可以帮助解释。 
1. 【feature】支持将前端结果保存到服务器的数据库中，这样用户保存后，就可以直接进入
1. 【feature】支持设置生成视频模型，支持sora
1. 【feature】前端自动化测试，还需要通过claude code的方式实现自动化测试，还需要自动化测试结果反馈。
1. 【bug】前端自动化测试，还需要通过claude code的方式实现自动化测试，还需要自动化测试结果反馈。
1. 【feature】图片编辑
1. 【bug】发现sora只支持 16：9 或 9：16比例
1. 【bug】修复视频节点，上传时候后，重新载入视频节点会丢失的问题
1. 【feature】时间轴 支持 视频节点
1. 【feature】支持时间轴的节点剪切功能，支持节点的预览
1. 【feature】支持导入剧本，自动生成分镜组节点。注意，生成的分镜组 含有多个分镜，各个分镜的时长加起来，差不多15s左右。默认使用阿里云百炼作为llm。
1. 【feature】时间轴支持 音频节点的添加，替换，移动
1. 【feature】对接tts
1. 【feature】项目支持通过风格图片，或者文字来控制画风
1. 【bug】分镜组的节点名称 应该是分镜组，而不是分镜组的 id
1. 【feature】支持场景库中的导入功能，支持上传场景的参考图、场景文字信息。
1. 【feature】支持角色库中的导入功能，支持上传角色的参考图、角色文字信息，角色参考音频。
1. 【feature】剧本拆分出来的 镜头内容，所有角色必须用【【】】括起来。
1. 【feature】场景支持嵌套配置。支持将llm的场景和db的场景关联起来
1. 【feature】镜头节点支持天气和时间的配置
1. 【feature】独立分镜节点，要参考场景 以及 角色的图片 生成对应的分镜图。
1. 【bug】分镜节点，没有支持加载后自动恢复的功能
1. 【feature】分镜节点，生成对应的图片，需要参考场景图片。
1. 【feature】分镜组节点 支持选择不同的生成分镜图的模型。
1. 【feature】分镜节点，生成的图片，需要支持 文字来控制画风的功能。发现使用参考图 控制画风，效果不佳，会导致生成的图片内容参考了 画风参考图的内容。
1. 【feature】连接着分镜节点，可以生成视频。
1. 【feature】根据分镜组节点，生成对应的分镜图（支持一个镜头 或者 多个镜头 一个图片）
1. 【bug】提醒大模型，生成剧本时，人物的外貌描写可以尽可能省略，系统的角色库中已有外貌信息，如果随意描写可能导致和角色库的外貌信息冲突。
1. 【feature】镜头节点，视频提示词需要改为文本的形式。
1. 【feature】此外需要支持调用llm将可能违规的内容替换掉。
1. 【bug】分镜图的 长宽 比例不对，应该根据分镜的数量，来推测具体哪个比例。
1. 【bug】点击创建的节点时，节点创建后可以在视野的左边，而不是画布的最左边
1. 【bug】分镜节点，参考的图片需要有上限，不允许超过5个。
1. 【feature】【p1】支持角色生成sora character，支持角色参考图生成视频。
1. 【feature】镜头节点生成视频的部分需要测试用例。
1. 【feature】【p1】支持基于角色的视频，创建角色卡。
1. 【bug】部分角色的视频创建成功后，还是 创建Sora角色卡的按钮依然是无法点击的。
1. 【bug】角色节点 生成角色视频后，视频和角色的连接线没有，重新加载后也不存在。似乎创建角色卡后，不会同步到数据库。
1. 【feature】【p1】基于镜头节点生成视频时，将角色替换为sora character。
1. 【feature】【p1】画布向外扩容后，但是线条的可见区域没有变化，导致扩容出去的部分，线条不可见。
1. 【feature】【p1】支持导出剪影的工程文件，以及完整的视频导出。
1. 【bug】删除多余的素材库
1. 【bug】用户登出后，还可以 添加场景时，看到不同的场景信息。还可以看到不同的角色。
1. 【bug】用户登出后，还可以 上传视频和图片。
1. 【feature】【p2】剪影草稿生成，素材的下载时，需要识别素材是否在本地，如果是本地，则不需要通过网络下载。
1. 【bug】【s1】偶发性的上传的视频无法下载。
1. 【bug】【s2】如果是同一个视频，但是两次在时间轴里，就会导致生成同一个视频多次出现在素材里。 
1. 【feature】【p2】需要支持批量选择节点。用户点击ctrl键时，鼠标会进入指针模式，该模式下，可以按下左键，出现方形的选择框，被选中的多个节点，支持批量移动，或者批量删除。
1. 【feature】【p1】支持在画布查看算力剩余
1. 【feature】【p2】点击处于z-index下面的节点时，该节点的z-index应该暂时提升，方便查看到
1. 【deploy】生成环境数据库、配置文件添加和修改
1. 【bug】【s1】节点拖动不方便
1. 【deploy】发布v1.0版本
1. 【feature】【p1】支持微信浏览器内算力购买
1. 【bug】【s2】开始时间轴后，需要将地图上移。
1. 【feature】【p2】支持元素重新排列后
1. 【bug】【s2】批量生成分镜图时，节点布局 非常得乱，生成得节点，应该尽量找空闲得位置去放置。
1. 【feature】【p1】支持其他浏览器内算力购买
1. 【bug】【s1】注册和登录太麻烦，注册密钥只需要输入一次，注册完毕后直接登录成功。登录记录是否同意使用手册后，下次就直接打勾。
1. 【deploy】全流程测试
1. 【feature】【p2】剧本节点需要支持以下选项， 1. 强制对话内容强制中景(半身像)，避免sora全景是对话效果不佳。 2. 不需要背景音乐，方便后期调音 3. 不需要任何字幕，避免出现画面乱码。

2026-1-4 上线:

1. 【feature】【p2】支持ctrl+z 恢复节点
1. 【feature】【p1】所有的js都需要被混淆，防止二次开发
1. 【bug】【s0】剧本节点拆分分镜图时，如果没有选择世界，就弹出弹框确认。
1. 【bug】【s1】世界名称不允许重复，否则世界选择之后，就不知道匹配哪个世界了
1. 【bug】【s2】修复了角色节点点击生成sora角色卡 时失败的问题
1. 【feature】【p2】新增新用户向导，引导用户使用工作流。
1. 【bug】【s1】发现如果分镜中存在没有的角色，就会导致系统无法生成分镜图
1. 【feature】【p2】世界节点支持编辑、删除。
1. 【bug】【s2】剧本禁止生成全景的逻辑，没有应用到 图片提示词中。
1. 【bug】【s2】修复了分镜节点，如果在错误世界时，会见分镜的图片提示词 不匹配的角色 【【】】去掉，但是没有更新页面的问题


2026-1-5 上线:

1. 【bug】新手引导5/9的时候，剧本多个时，存在问题，每一步新手引导需要支持跳过。
1. 【bug】【s2】剧本拆分多个分镜组时，如果存在多个分镜组，只需要随机选择一个分镜组就行，而不是所有地分镜组都点击才退出新手指引。
1. 【bug】【s2】新手指引第一步的左上角 + 号按钮的绿色聚焦，不够显眼。另外，在选择世界这边，还有一个 聚焦框，这个是错误的，应该是到 到+号按钮。
1. 【bug】修复了工作流列表界面，点击编辑按钮后，世界字段没有和数据库中的同步
1. 【bug】【s2】多个分镜节点时，只需要随机选择一个分镜就行，而不是所有的分镜都点击生成视频才退出新手指引。
1. 【bug】【s3】修复了弹出的角色弹窗中 纵向长度过长，导致按钮无法看到的问题
1. 【bug】【s3】修复了剧本节点 拆分分镜组时，分镜组之间 纵向太过拥挤的问题
1. 【bug】【s3】修复了分镜组节点，拆分分镜按钮时，分镜和分镜之间的间距也太小了的问题


2026-1-8 上线:

1. 【bug】【s1】登录失效时，工作流页面需要自动跳转重新页面。
1. 【feature】【p2】支持生成音频功能，实现了音频组功能。
1. 【feature】【p2】支持音频文件上传到线上，这样下次重新加载页面时就可以直接复用。
1. 【feature】【p2】支持音频情感向量控制，支持了情感参考音频。
1. 【feature】【p2】声音合成需要支持参考视频链接功能，并设置视频时长不能超过20s，大小不能超过40M。
1. 【feature】【p2】支持对话组节点支持将生成的视频作为情感参考音频。
1. 【feature】【p2】支持分镜节点，增加生成音频的按钮，点击后自动生成对话组节点。
1. 【bug】【s2】修复了对话组的情感参考音频，用连接线时，期望只有一个视频节点可以和它相连，现在可以有多个视频节点和它相连。
1. 【bug】【s2】修复了生成分镜时，镜头顺序不连续的问题。
1. 【feature】【p2】支持对话组节点 将音频轴添加到时间轴的功能。
1. 【feature】【p2】时间轴导出，兼容中间没有视频的部分。
1. 【bug】【s2】如果没有对话，那么镜头节点生成对话组的按钮不可点击。
1. 【feature】【p2】如果未知角色，无法生成音频文件。
1. 【bug】【s3】角色的参考音频不能超过 20秒。
1. 【bug】【s1】如果存在角色存在sora角色卡，再用首帧图生成视频时，就很基本会失败。考虑只有文生视频节点支持使用角色卡。
1. 【feature】【p2】支持剧本拆分时，强制把对话人物设置为单人。
1. 【deploy】【p1】修改其他环境配置文件，增加test_mode参数
1. 【deploy】【p1】v1.2 版本发布

2026-1-9 上线:

1. 【feature】【p1】列表界面对接LTX2.0、wan2.2接口。
1. 【bug】【s2】实现了runninghub的槽位机制，避免任务由于等到槽位而被错误关闭。
1. 【feature】【p1】列表界面对接可灵接口。
1. 【feature】【p1】支持工作流页面，调用wan2.2、LTX2.0、可灵接口。
1. 【deploy】【p1】其他环境 配置文件新增 max_concurrent_slots 参数，数据库新增表runninghub_slots，并在根目录创建logs目录。
1. 【feature】【p1】runninghub接口需要进行数量控制，不能超过3个。


2026-1-24 上线：

1. 【bug】【s2】合并分镜的分镜组，不支持生成对话组节点。
1. 【bug】【s2】合并分镜的分镜视频和音频，无法加入到时间轴中。
1. 【bug】【s2】生成分镜图时，需要在末尾增加画风描述。
1. 【bug】【s2】道具节点，存在图片时，在编辑界面没有正确显示。
1. 【feature】【p2】为了保证画风一致，需要将对应的角色、场景、道具等同统一画风，才能做出比较好的效果。
1. 【feature】【p2】支持自动生成剧本。
1. 【deploy】【p2】world以及script 表结构变化

视频工作流todo:

1. 【feature】【p2】在地图的上方增加一个 传送按钮，点击后会自动出现从剧本向下的树状结构，罗列出所有的分镜节点，点击后可以快速将视角移动过去。
1. 【feature】【p2】图片涂色编辑，镜头三轴跳转。
1. 【milestone】【p1】研究剧本功能整合、角色功能整合、场景功能整合、道具功能整合。研究画风统一。
1. 【feature】【p2】支持高清放大节点。
1. 【feature】【p2】sora生成的视频的remix功能
1. 【reasearch】【p2】保持分镜图一致性 方案一：研究gemini的连续对话功能，对于保持画面一致性是否有帮助？
1. 【feature】【p2】调研有什么办法可以让生成的场景图不越轴。
1. 【feature】【p2】研究生成分镜组时，第一个镜头就是空镜头，因为sora生成第一个镜头肯定时废的，第二个镜头可能有用。 测试下提示词 用户强制性要求，第一个镜头必须是是上个分镜结尾的转人场，0-2秒时间，本分镜从2秒开始。
1. 【feature】【p2】支持导出剪影时，强制把画面拉大到覆盖整个画面。
1. 【feature】【p2】画风内容支持根据图片来提取。
1. 【reasearch】【p3】保持分镜图一致性 方案二：研究九宫格分镜图，nano banana使用pro，生成4k的分镜图，并支持通过软件类进行切分。
1. 【reasearch】【p1】研究sora用线稿图生成是，图1是空白，图2是前一个分镜组的末尾图片，图3是当前分镜组的首帧图，这种方式，压榨sora的后续视频。
1. 【reasearch】【p2】研究对口型的功能（动画/视频）
1. 【feature】【p1】出图研究使用z-image、qwenimageedit
1. 【feature】【p2】支持角色自动提取，生成角色参考图，角色文字信息，角色参考音频等。
1. 【feature】【p2】支持道具功能。
1. 【feature】【p2】支持sora2自动切白，将sora2生成的视频的前面静止部分剪切掉。
1. 【feature】【p1】支持真人角色创建线稿图后，再生成角色
1. 【bug】【s2】为什么登录失效了，但是工作流列表页面依然有值？
1. 【feature】【p2】分镜组支持修改，比如对某个镜头不满意，可以AI重新修改
1. 【feature】【p1】优化现有的将角色替换为sora character的逻辑需要兼容现有的提示词模板。
1. 【feature】【p2】新增问题反馈按钮
1. 【feature】【p2】工作流支持 挂机任务，支持生成好分镜图的分镜节点，自动生成 N个视频、最多允许失败几次。
1. 【feature】【p1】智能体:支持导入视频，进行自动提取分镜，并自动生成后续视频。
1. 【feature】【p2】在选择模型中，增加每个模型平均时长。
1. 【feature】【p2】支持公共的音频感情库，公共的音色库，供音频合成生成使用。
1. 【feature】【p2】分镜是不是横着，sora2理解效果会比较好？
1. 【bug】【s2】没有完全修复，需要所有弹框出现后，当点击鼠标，按下时在弹窗内，松开鼠标时，在弹框外的情况，不要让弹窗消失。
1. 【feature】【p2】支持算力日志查看
1. 【feature】【p1】对接wan2.6接口
1. 【feature】【p2】支持AI质检，自动检查分镜图 人物是否相同，画风是否一致。对话的场景下，是否出现越轴。
1. 【feature】【p2】支持场景自动提取，生成场景参考图，场景文字信息。
1. 【feature】【p2】支持音频合成后对口型功能。
1. 【feature】【p1】视频说话顺序控制（检查sora的角色控制）。
1. 【feature】【p2】支持一键生成分镜图。
1. 【deploy】【p2】将https://ailive.perseids.cn 打到服务器
1. 【feature】【p2】支持角色、场景删除
1. 【feature】【p3】需要有一个机制删除/tmp/jianying_export/前一天的草稿文件。 
1. 【feature】【p3】支持算力有月的有效期
1. 【bug】【s2】时间轴移动后，应该只调整尽可能少的元素，而不是整个都重新渲染一遍。
1. 【bug】【s3】点击连接线后，连接线的删除 按钮，在点击其他元素后不会消失。
1. 【deploy】【p2】考虑新建功能说明文档，让每次修改后都同步更新文档内容。
1. 【feature】【p2】导出剪影草稿时，视频需要保持 1080p分辨率，如果是sora生成的，给它自动拉大一点，不要留黑边。
1. 【feature】【p3】支持AI字幕生成

